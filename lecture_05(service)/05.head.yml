apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-head-nginx
  labels:
    app: deploy-head-nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: deploy-head-nginx
  template:
    metadata:
      labels:
        app: deploy-head-nginx 
    spec:
      containers:
      - name: nginx
        image: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: head-nginx 
spec:
  selector:
    app: deploy-head-nginx  
  ports:
    - name: http
      port: 80
      targetPort: 80
  clusterIP: None